import { AnchorClickDirective } from './anchor-click.directive';

describe('AnchorClickDirective', () => {
  it('should create an instance', () => {
    const directive = new AnchorClickDirective();
    expect(directive).toBeTruthy();
  });
});

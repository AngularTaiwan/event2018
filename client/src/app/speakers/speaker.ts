export interface ISpeaker {
  id: string;
  img: string;
  keynote?: boolean;
  name: string;
  company: string;
  title: string;
  about: string;
  hide: boolean;
  fb?: string;
  twitter?: string;
  github?: string;
  website?: string;
}
